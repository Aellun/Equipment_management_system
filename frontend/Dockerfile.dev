# Development image — hot-reload via `next dev`. Used by docker-compose.dev.yml.
FROM node:20-alpine
WORKDIR /app

COPY package*.json ./
RUN npm install

COPY . .

EXPOSE 3000
CMD ["npm", "run", "dev"]

/** @type {import('next').NextConfig} */
const nextConfig = {
  // Produces a minimal, self-contained server build in .next/standalone
  // for fast, small production images.
  output: "standalone",
};

export default nextConfig;
